name: TsaoDFT quality gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Final hosted-verification instrumentation in progress; permanent CI is restored in the next verified commit."
