name: Apply isolated mypy repairs

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-mypy-fixes-once.yml

permissions:
  contents: write
  statuses: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout main
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Python 3.12
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-dev.txt

      - name: Install development tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Capture repairs
        id: repairs
        shell: bash
        run: |
          set +e
          {
            python .github/apply-mypy-fixes-once.py
            python .github/fix-mypy-walrus-once.py
            python -m ruff format .
            python -m ruff check .
            python scripts/run_type_checks.py
          } 2>&1 | tee mypy-repair.log
          code=${PIPESTATUS[0]}
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload repair log
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: mypy-repair-${{ github.sha }}
          path: mypy-repair.log
          if-no-files-found: error
          retention-days: 7

      - name: Publish repair status
        if: always()
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          RESULT: ${{ steps.repairs.outputs.exit_code }}
        run: |
          state=failure
          description='Isolated mypy repair failed'
          if [ "$RESULT" = '0' ]; then
            state=success
            description='Isolated mypy repair passed'
          fi
          gh api --method POST "repos/$GITHUB_REPOSITORY/statuses/$GITHUB_SHA" \
            -f state="$state" \
            -f context='tsao-dft/mypy-repair' \
            -f description="$description" \
            -f target_url="https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"

      - name: Remove one-time files and commit
        if: steps.repairs.outputs.exit_code == '0'
        run: |
          rm .github/apply-mypy-fixes-once.py
          rm .github/fix-mypy-walrus-once.py
          rm .github/workflows/apply-mypy-fixes-once.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'refactor: resolve isolated mypy findings [skip ci]'
          git push origin HEAD:main

      - name: Fail when repairs failed
        if: steps.repairs.outputs.exit_code != '0'
        run: exit 1
