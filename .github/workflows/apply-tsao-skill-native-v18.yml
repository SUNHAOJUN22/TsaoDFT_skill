name: Stage complete skill-native V18 payload

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging-paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'V18 payload staging is intentionally paused; no source mutation is performed.'
