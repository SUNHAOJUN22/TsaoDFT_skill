name: CI maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Repository maintenance in progress; push CI will be restored in the final validated commit."
