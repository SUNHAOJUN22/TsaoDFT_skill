name: TsaoDFT quality gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Repository-wide quality audit in progress; permanent CI is restored after verified repairs."
