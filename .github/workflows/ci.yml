name: One-shot coverage artifact isolation patch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: "${{ github.event_name == 'push' && github.event.head_commit.message == 'ci: apply coverage artifact isolation patch' }}"
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout full history
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
      - name: Apply isolation patch
        run: |
          python .github/patch_coverage_isolation.py
          python -m py_compile tests/test_release_coverage_runner.py
      - name: Commit target and remove patcher
        shell: bash
        run: |
          set -o pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/patch_coverage_isolation.py
          git add tests/test_release_coverage_runner.py
          git commit -m "test: isolate simulated coverage reports"
          for attempt in 1 2 3; do
            git fetch origin main
            git rebase origin/main
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
